import { useReducer } from "react";

const ApplicationRouter = () => {
    const routers = useReducer([
        // {path: "/", element: ""}
    ])
    
    return routers;
}
 
export default ApplicationRouter;